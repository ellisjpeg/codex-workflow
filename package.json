{
  "name": "codex-workflow",
  "version": "0.5.7",
  "description": "A version-guarded, native-style Workflow extension for Codex Desktop on macOS.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "author": "ellisjpeg",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ellisjpeg/codex-workflow.git"
  },
  "bugs": {
    "url": "https://github.com/ellisjpeg/codex-workflow/issues"
  },
  "homepage": "https://github.com/ellisjpeg/codex-workflow#readme",
  "engines": {
    "node": ">=24.0.0"
  },
  "keywords": [
    "codex",
    "electron",
    "macos",
    "workflow"
  ],
  "scripts": {
    "install:patch": "node scripts/install.mjs",
    "reapply:patch": "node scripts/install.mjs --reapply",
    "recover:patch": "node scripts/install.mjs --recover",
    "status": "node scripts/status.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check runtime/main.cjs && node --check runtime/preload.cjs && node --check runtime/updater.cjs && node --check loader.cjs && node --check scripts/lib.mjs && node --check scripts/install.mjs && node --check scripts/install-runtime.mjs && node --check scripts/status.mjs && node --check scripts/uninstall.mjs && npm test",
    "uninstall:patch": "node scripts/uninstall.mjs"
  },
  "dependencies": {
    "@electron/asar": "4.3.0"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
